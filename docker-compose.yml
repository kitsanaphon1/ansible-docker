# docker-compose.yml
version: '3.8'

services:
  ansible:
    build: .
    container_name: ansible-azure
    volumes:
      - ./ansible:/ansible
      - ~/.ssh:/root/.ssh:ro
    working_dir: /ansible
    entrypoint: /bin/bash
